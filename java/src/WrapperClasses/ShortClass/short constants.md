
Список констант класу обгортки `Short`:

1. `MAX_VALUE`: Максимальне значення для типу `short`.
2. `MIN_VALUE`: Мінімальне значення для типу `short`.
3. `SIZE`: Розмір типу `short` в байтах.
4. `BYTES`: Розмір типу `short` в байтах.
5. `TYPE`: Об'єкт класу `Class` для типу `short`.
